# TUI 界面演示 - 完整交互版

## 启动方式

```bash
./target/release/offline-analysis-agent --gui tui
```

## 界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│ 离线分析 AI Agent v0.1.0                                        │
├─────────────────────────────────────────────────────────────────┤
│ [任务管理] │ 元数据 │ 创建向导 │ 数据质量 │ 设置                │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 任务列表 (共 3 个) | ↑/↓:选择 | Enter:运行 | e:编辑 | Del:删除│ │
│ │                                                              │ │
│ │ ▶ sync_user_daily [增量同步] - 每日用户数据同步             │ │
│ │   状态：Success | 更新：2026-05-15 02:05:00                  │ │
│ │                                                              │ │
│ │   agg_order_hourly [单表聚合] - 每小时订单聚合              │ │
│ │   状态：Running | 更新：2026-05-15 03:30:00                  │ │
│ │                                                              │ │
│ │   dwd_user_profile [全量同步] - 用户画像全量同步            │ │
│ │   状态：Pending | 更新：2026-05-15 04:00:00                  │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│ q:退出 | Tab:切换 | ↑/↓:选择 | Enter:运行 | e:编辑 | Del:删除   │
└─────────────────────────────────────────────────────────────────┘
```

## 交互功能

### 任务管理

| 按键 | 功能 |
|------|------|
| `↑` / `↓` | 选择任务 |
| `Enter` | 运行任务（弹出确认对话框） |
| `e` | 编辑任务名称（输入模式） |
| `Del` | 删除任务（弹出确认对话框） |

**确认对话框**：
```
┌──────────────────────────────┐
│ 运行任务                     │
│                              │
│ 确定要运行任务               │
│ 'sync_user_daily' 吗？       │
│                              │
│  [Y] 确认  |  [N] 取消       │
└──────────────────────────────┘
```

**输入模式**：
```
┌──────────────────────────────┐
│ 任务名称                     │
│                              │
│ sync_user_daily▌             │
│                              │
│  Enter:确认 | Esc:取消       │
└──────────────────────────────┘
```

**消息弹窗**：
```
┌──────────────────────────────┐
│ 成功                         │
│                              │
│ 任务名称已更新为：           │
│ sync_user_daily              │
│                              │
└──────────────────────────────┘
```

### 元数据浏览

```
┌──────────────────┬─────────────────────────────────┐
│ 数据库           │ 表 (ods.users)                  │
├──────────────────┼─────────────────────────────────┤
│   default        │ ▶ users (表)                    │
│ ▶ ods            │   orders (表)                   │
│   dwd            │   products (表)                 │
│   dwm            │                                 │
└──────────────────┴─────────────────────────────────┘
```

| 按键 | 功能 |
|------|------|
| `↑` / `↓` | 选择数据库 |

### 创建向导

显示 7 步创建流程：
1. 选择任务模板
2. 填写任务信息
3. 选择数据表
4. 配置调度
5. 质量规则
6. 预览配置
7. 提交创建

### 数据质量

展示 8 类数据质量检查：
- 非空检查 - 验证字段不为 NULL
- 唯一性检查 - 验证字段值唯一
- 值域检查 - 验证数值在范围内
- 枚举值检查 - 验证值在枚举列表中
- 波动检查 - 验证数据波动在阈值内
- 行数检查 - 验证表行数在预期范围
- 格式检查 - 验证数据格式 (日期/邮箱等)
- 一致性检查 - 验证跨表数据一致性

### 系统设置

- Azkaban 连接配置
- Hive Metastore 配置
- Git 仓库配置
- AI 服务配置
- 缓存策略

## 全局快捷键

| 按键 | 功能 |
|------|------|
| `q` | 退出应用 |
| `Tab` | 切换标签页（向前） |
| `Shift+Tab` | 切换标签页（向后） |
| `←` / `→` | 切换标签页 |

## 状态指示

| 状态 | 颜色 | 说明 |
|------|------|------|
| Success | 绿色 | 任务执行成功 |
| Running | 黄色 | 任务正在运行 |
| Pending | 灰色 | 任务等待执行 |
| Failed | 红色 | 任务执行失败 |
| Killed | 紫色 | 任务被终止 |

## 技术特性

- ✅ 输入框组件（支持光标移动、删除）
- ✅ 确认对话框（Y/N 选择）
- ✅ 消息弹窗（成功/错误/信息）
- ✅ 列表选择（高亮选中项）
- ✅ 分栏布局（元数据浏览）
- ✅ 模态弹窗（阻止背景交互）
