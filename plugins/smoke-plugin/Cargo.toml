[package]
name = "smoke-plugin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Smoke test WASM plugin for hive-claw Agent Runtime — demonstrates capability-based host_call ABI"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
extism-pdk = "1"
serde = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
