//! DAG 可视化视图（Mock）

/// DAG 视图结构
pub struct DagView;

impl DagView {
    pub fn new() -> Self {
        Self
    }
}

impl Default for DagView {
    fn default() -> Self {
        Self::new()
    }
}
