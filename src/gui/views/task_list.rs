//! 任务列表视图（Mock）

/// 任务列表视图结构
pub struct TaskListView;

impl TaskListView {
    pub fn new() -> Self {
        Self
    }
}

impl Default for TaskListView {
    fn default() -> Self {
        Self::new()
    }
}
