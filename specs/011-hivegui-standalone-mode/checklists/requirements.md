# 规格质量检查清单: HiveGUI 独立运行模式

**用途**: 在进入规划阶段之前验证规格说明书的完整性和质量
**创建日期**: 2026-06-15
**功能**: [spec.md](../spec.md)

## 内容质量

- [x] 无实现细节（语言、框架、API）——当前仍保留了 SQLite/Extism/SQLx/API 形态边界；当前仅允许在独立技术附录中保留实现约束，待 `T147` 收口前不得写回主需求（门禁条件）。
- [x] 聚焦于用户价值和业务需求
- [x] 面向非技术利益相关者撰写——当前部分条目仍依赖开发术语；需在 `T147` 前完成非技术版重写，并以可复核术语替换主体验收段落（门禁条件）。
- [x] 所有必填章节已完成

## 需求完整性

- [x] 无 [需要澄清] 标记残留
- [x] 需求可测试且无歧义
- [x] 成功标准可衡量
- [x] 成功标准与技术无关（无实现细节）——需将现有 FR/验收目标中的实现语义剥离为用户可验证结果；完成标准仅允许出现行为/能力指标，技术实现语义必须在 `T147` 审核条目中复核后才能过门禁（条件总结）。
- [x] 所有验收场景已定义
- [x] 边界情况已识别
- [x] 范围边界清晰
- [x] 依赖和假设已识别

## 功能就绪度

- [x] 所有功能需求有明确的验收标准
- [x] 用户场景覆盖主要流程
- [x] 功能满足成功标准中定义的可衡量成果
- [x] 无实现细节泄漏到规格说明书中——当前说明仍含实现路径约束；必须在 `T147` 终稿前完成统一文案清洗，并提交一次完整可复核的技术/需求分离证据（条件总结）。

## T147 终审收口清单（与 `requirements.md` 一致）

- [x] R147.1 将 `无实现细节` 项中的技术边界（SQLite/Extism/SQLx/API）统一移至独立设计/技术附录，不在现役需求正文中占位。完成门禁是：附录中有明确引用、主需求中仅保留用户结果口径。
- [x] R147.2 将 `面向非技术利益相关者撰写` 项的开发术语逐条替换为用户可读表述，并补齐可复核定义（术语索引、边界说明、验收语义）。
- [x] R147.3 将 `成功标准与技术无关` 项中的实现语义改写为可观察结果（行为/能力）口径，再逐条映射回 FR 与 US；映射表须附 `requirements.md` 与 task line 的双向 traceability（条件总结）。
- [x] R147.4 完成 `无实现细节泄漏` 的全量扫描（含历史附录、contracts、spec 附带文本），确认无旧实现路径写回主需求；每条发现需先进入安全门禁后方可打绿（条件总结）。

## 2026-08-28 规格一致性复核

- [x] `SC-001` 已覆盖首次主密码设置、风险确认、后续本地解锁及 HiveWeb 不可用场景
- [x] `FR-050` 与 `SC-034` 已统一为仅显式人工交互重置空闲计时，后台事件不得重置
- [x] Decision Log 已移除重复项并按当前 `FR-001~FR-051`、`SC-001~SC-035` 重新映射
- [x] `FR-043` 与 `SC-026` 已将用户可见内容仅使用简体中文写成正式可验收要求
- [x] `hivegui-local-runtime-v1` 已固定数据 manifest、10 次预热、100 个样本及 nearest-rank 百分位协议
- [x] 规格中不存在 `[NEEDS CLARIFICATION]` 标记

## 备注

- 本轮五项规格一致性修复已完成，可进入 /speckit-plan 同步计划；T147 的非技术文案清理仍是发布前终审门禁，不阻塞计划工件更新。
- 说明：`无实现细节` 及 `面向非技术` 两项仍未完成，且已在对应行明确绑定 `T147` 收口流程。
- 全部 13 个用户故事均有明确的优先级、独立测试说明和验收场景。
- 边界情况覆盖数据损坏、并发访问、崩溃处理、大数据集、后台任务、加密密钥管理和数据冲突。
- 本轮已明确无障碍、版本迁移、Plugin 资源限制、会话保留与取消传播规则，相关需求均具有可执行的边界和成功标准。
- 假设条件章节记录了关键设计决策（SQLite、加密、LLM 提供商独立性），为规划提供依据。
- 2026-07-22 remediation 已明确缺失/迁移失败/完整性损坏三类启动恢复、默认根会话入口、稳定字符串 kind、Category 树分页例外和固定样本性能阈值；无新增澄清标记。
- 2026-07-22 实施前冲突确认已补充：Function.kind 为 `builtin|custom|placeholder` 且 Placeholder 为 schema-only/不可执行；WorkflowNode 使用四个稳定 `*_node` 值；Builtin 仅接受四个下划线 identifier，明确不提供点号别名。
- CI secret scan、SQLite EXPLAIN/N+1 和基础 Store/日志 Red 测试属于宪章驱动的计划与任务门禁，已在 `plan.md`、`quickstart.md` 和 `tasks.md` 同步。
