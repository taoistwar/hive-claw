# 2026-08-15

## T145 依赖安全门禁（`cargo deny check advisories`）收尾

- 网络恢复（用户开了代理，之前 `rsproxy.cn` 无法解析现已可访问）。
- 复查上游：经 crates.io API 确认 `aws-sdk-s3` 最新即 `1.141.0`（2026-08-06）、`extism` 最新即 `1.30.0`（2026-06-04），二者均为各自最新发布版，但上游**仍未发布**放宽 `lru`/`wasmtime` 约束的修复版本。升级路径不存在（lru 被 aws-sdk-s3 锁 `^0.16.3`；wasmtime 被 extism 锁 `^43`）。
- 处置：在 `deny.toml` 的 `[advisories]` 增加 `ignore = ["RUSTSEC-2026-0253", "RUSTSEC-2026-0222"]`，附 PROVISIONAL 注释（上游无修复版、已评估使用面、待安全复核签字）。`RUSTSEC-2026-0255`(sized-chunks) 此前已用 `third_party/sized-chunks-0.6.5` 本地 patch 处理。
- 验证：`cargo deny check advisories` → `advisories ok`（EXIT 0）。
- 状态：advisories 门禁现已通过（临时豁免），T145 仍需 Feature 011 security checklist 两人审批签字才正式闭合。（**更新**：constitution 两人审批门槛已于 2026-08-17 被用户移除，T145 同日正式闭合，见 `2026-08-17.md` 与 `implementation-review.md`。）

## T145 整体状态（2026-08-15）
- `cargo clippy --locked --workspace --all-targets -- -D warnings`：通过（0 error，见 08-14 记录）。
- `cargo deny check advisories`：通过（临时豁免，待签字）。（**更新**：已于 2026-08-17 闭合，见 `2026-08-17.md`。）
- 未闭合：T145 仍需安全复核签字（governance 要求，非技术阻塞）。（**更新**：constitution 门槛 08-17 移除，T145 已闭合。）

## 环境备注
- 依赖拉取走 `rsproxy-sparse`（registry 被替换）。`cargo search` 需 `--registry crates-io` 或不可用；升级用 `cargo update -p <crate> --precise <ver>`。
- 用户通过代理访问外网；无代理时 `rsproxy.cn` DNS 解析失败。

## `cargo deny check licenses` 质量门修复（2026-08-15）

- 跑全量 `cargo deny check` 发现 `deny.toml` 原本**缺失 `[licenses]` 段**（默认拒绝所有许可），且内部 crate 缺 `license` 字段。
- 修复：
  - 16 个内部 workspace crate（`agent/api/bus/channels/cli/command/config/cron/heartbeat/nanobot/providers/security/session/skills/templates/utils`）补 `license.workspace = true`（继承 `[workspace.package]` 的 `Apache-2.0`）。注意 `config`/`templates`/`nanobot` 的 `edition` 是 `= "2024"` 形式（非 `.workspace`），需单独补。
  - `deny.toml` 新增 `[licenses]` 段，allow 列表含 permissive 许可：`MIT, Apache-2.0, BSD-2/3-Clause, ISC, MPL-2.0, Zlib, CC0-1.0, Unicode-3.0, Unlicense, GPL-3.0-or-later, Apache-2.0 WITH LLVM-exception, BSL-1.0, 0BSD, CDLA-Permissive-2.0, bzip2-1.0.6`。`include-dev = false`。
  - **`GPL-3.0-or-later` 经用户明确批准**加入（来自 gpui→sum_tree 的传递依赖，copyleft 风险已评估）。
- 验证：`cargo deny check licenses` → `licenses ok`（EXIT 0）。`bans`/`sources`/`advisories` 均 ok。
- 注：全量 `cargo deny check`（不带子命令）会尝试从 github.com 更新 advisory-db，当前代理不通 github 故失败；但各子命令（advisories 用本地缓存 DB、licenses/bans/sources 不走 github）均通过。
- T145 技术面现已全部通过（clippy + 4 项 deny 子检查），仅剩 governance 签字（待 Feature 011 security checklist 两人审批）。（**更新**：constitution 两人审批门槛 2026-08-17 被用户移除，T145 同日正式闭合。）
